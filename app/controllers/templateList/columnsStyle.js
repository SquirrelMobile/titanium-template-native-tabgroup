var args = $.args;

if (args.column) {
  for (var i = 0; i < parseInt(args.column); i++) {
    // $.content.add(
    //   $.UI.create("ImageView", {
    //     width: args.column / 100 + "%",
    //     height: Ti.UI.FILL,
    //     bindId: "elem" + i
    //   })
    // );
  }
}
